name: 'My Custom Action'
description: 'This action prints a message'
runs:
  using: 'composite'
  steps:
    - run: |
        echo "Hello from the custom action!"
        out_put="testing it"
        echo "out_data=$out_put" >> $GITHUB_OUTPUT
      shell: bash
